[Unit]
Description=MCP Servers
StopWhenUnneeded=true

[Kube]
Yaml=mcp-servers.yaml
AutoUpdate=local

[Service]
Restart=on-failure
RestartSec=5

[Install]
# No WantedBy — pulled in by llama-server via BindsTo
